prompt_ru_1 = "На картинке чек из магазина. Сделай распазнование текста и выведи результат в формате JSON: {'store': '...', 'date': '...', 'items': [{'name': '...', 'price': '...'}, ...], 'total': '...', 'ALV': []}"
prompt_ru_2 = "The image shows a receipt from a store. Perform text recognition and output the result in JSON format: {'store': '...', 'date': '...', 'items': [{'name': '...', 'price': '...'}, ...], 'total': '...', 'ALV': []}"
prompt_ru_3 = "На картинке чек из магазина. Сделай распазнование текста и выведи результат в формате JSON, если не можешь распознать кокое-то поле или текст, то оставь поле пустым. Выведи в формате JSON: {'store': '...', 'date': '...', 'items': [{'name': '...', 'price': '...'}, ...], 'total': '...', 'ALV': [{'percentage': '...', 'without_alv': '...', 'with_alv': '...', 'total': '...'}, ...]}"
prompt_ru_4 = """
Ты — система для точного извлечения данных из чеков (OCR + parsing).

Твоя задача:
1. Внимательно прочитать текст на изображении чека
2. Извлечь только те данные, которые явно присутствуют
3. НЕ додумывать и НЕ изменять числа
4. Если значение отсутствует или неразборчиво — оставить пустую строку ""

ВАЖНО:
- Все числа должны быть извлечены ТОЧНО как в чеке
- Не пересчитывай и не исправляй значения
- Не объединяй и не дублируй поля
- Если ALV (налог) не указан явно — оставить массив пустым []
- Всегда возвращай ОДИН валидный JSON без комментариев

Формат ответа строго такой:

{
  "store": "",
  "date": "",
  "items": [
    {
      "name": "",
      "price": ""
    }
  ],
  "total": "",
  "ALV": [
    {
      "percentage": "",
      "without_alv": "",
      "with_alv": "",
      "total": ""
    }
  ]
}

Правила извлечения:

- "store" — название магазина
- "date" — дата покупки (как в чеке, не менять формат)
- "items":
  - "name" — название товара
  - "price" — цена товара (как указано)
- "total" — итоговая сумма

ALV (налоги):
- Извлекать только если явно есть строка с налогом (например ALV, VAT)
- Для каждой ставки создать отдельный объект
- Не вычислять значения самостоятельно

Если есть сомнения в числе — оставь поле пустым.

Ответ: только JSON, без пояснений.

"""


prompt_ru_5 = """
Ты — система для точного извлечения данных из чеков (OCR + parsing).

Твоя задача:
1. Внимательно прочитать текст на изображении чека
2. Извлечь только те данные, которые явно присутствуют
3. НЕ додумывать и НЕ изменять числа
4. Если значение отсутствует или неразборчиво — оставить пустую строку ""

ВАЖНО:
- Все числа должны быть извлечены ТОЧНО как в чеке
- Не пересчитывай и не исправляй значения
- Не объединяй и не дублируй поля
- Если ALV (налог) не указан явно — оставить массив пустым []
- Всегда возвращай ОДИН валидный JSON без комментариев

Формат ответа строго такой:

{
  "store": "",
  "date": "",
  "items": [
    {
      "name": "",
      "price": ""
    }
  ],
  "total": "",
  "ALV": [
    {...}
  ]
}

Правила извлечения:

- "store" — название магазина
- "date" — дата покупки (как в чеке, не менять формат)
- "items":
  - "name" — название товара
  - "price" — цена товара (как указано)
- "total" — итоговая сумма

ALV (налоги):
- Извлекать только если явно есть строка с налогом (например ALV, VAT)
- Для каждой ставки создать отдельный объект
- Не вычислять значения самостоятельно

Если есть сомнения в числе — оставь поле пустым.

Ответ: только JSON, без пояснений.

"""
